package com.heiyu.iot.sdk.sensor;

import java.util.Map;

/**
 * @author a007a
 * 传感器类型定义接口，有三个方法，读取数据，写入数据，执行动作并返回结果。
 * This interface denifite that a sensor contains three methods
 * lala
 * raddat
 *
 */
public interface Sensor {

    Map readData();
//    void setData(IOTdata action);
//    Map Actiong(IOTdata action);

}
